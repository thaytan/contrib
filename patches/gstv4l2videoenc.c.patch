--- gst-v4l2/gstv4l2videoenc.c	2019-10-17 09:48:11.318030719 +0200
+++ gst-v4l2/gstv4l2videoenc.c	2019-10-17 09:51:24.851490390 +0200
@@ -866,6 +866,9 @@
       klass->codec_name);
 
   allowed_caps = gst_pad_get_allowed_caps (GST_VIDEO_ENCODER_SRC_PAD (encoder));
+  /* Only renegotiate on upstream changes */
+  if(self->input_state)
+	  return TRUE;
 
   if (allowed_caps) {
 
